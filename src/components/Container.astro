---
interface Props {
  narrow?: boolean;
  class?: string;
}

const { narrow = false, class: className } = Astro.props;
---

<div
  class:list={[
    "mx-auto px-6",
    narrow ? "max-w-[760px]" : "max-w-[1100px]",
    className,
  ]}
>
  <slot />
</div>
